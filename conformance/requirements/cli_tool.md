# CLI Tool Fixture Requirements

Create a minimal command-line tool that prints a greeting.

## Constraints
- Provide a README with install and run instructions.
- Include unit tests for CLI behavior.
- Keep dependencies minimal and avoid telemetry.
