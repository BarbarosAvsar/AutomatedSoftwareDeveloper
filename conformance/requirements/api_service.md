# API Service Fixture Requirements

Build a minimal REST-style API service with a simple health/status endpoint.

## Constraints
- Provide a README with install and run instructions.
- Include unit tests and keep dependencies minimal.
- Ensure the project runs without secrets or external services.
