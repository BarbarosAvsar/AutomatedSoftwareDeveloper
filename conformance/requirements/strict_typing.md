# Strict Typing Fixture Requirements

Create a minimal CLI tool with strict typing enabled.

## Constraints
- Enforce strict type checking with mypy.
- Use minimal dependencies and no external services.
- Provide README install/run instructions.
