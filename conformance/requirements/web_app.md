# Web App Fixture Requirements

Scaffold a minimal web application with a static page and a tiny loader helper.

## Constraints
- Provide a README with install and run instructions.
- Include a simple build step and unit tests.
- Keep dependencies minimal and avoid external services.
