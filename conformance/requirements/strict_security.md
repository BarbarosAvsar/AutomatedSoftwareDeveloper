# Strict Security Fixture Requirements

Create a minimal CLI tool with strict security constraints.

## Constraints
- Security scanning must be enabled and required.
- No secrets or credentials are stored in artifacts.
- Use minimal dependencies and no external services.
