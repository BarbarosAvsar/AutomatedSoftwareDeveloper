# No Telemetry Fixture Requirements

Create a minimal CLI tool with telemetry disabled by default.

## Constraints
- Telemetry must be disabled by default.
- Use minimal dependencies and no external services.
- Provide README install/run instructions.
